---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: healthcheckrequests.health.aks.io
spec:
  group: health.aks.io
  names:
    kind: HealthCheckRequest
    listKind: HealthCheckRequestList
    plural: healthcheckrequests
    shortNames:
    - hcr
    singular: healthcheckrequest
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HealthCheckRequest is the Schema for the healthcheckrequests API.


          Created to request health monitoring for a specific target
          (node, node pool, or cluster). Monitoring apps watch for these
          and create corresponding HealthSignal resources with ownerReferences
          pointing back to the request.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HealthCheckRequestSpec defines the desired state of a HealthCheckRequest.
            properties:
              scope:
                description: |-
                  Scope indicates the level of this health check: Node, NodePool, or Cluster.
                  A Node-scoped request is created per node being replaced; a NodePool- or
                  Cluster-scoped request is created for the pool or cluster being upgraded.
                enum:
                - Node
                - NodePool
                - Cluster
                type: string
              targetRef:
                description: |-
                  TargetRef names the target of this health check: the node, node pool, or
                  cluster, according to Scope.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
            required:
            - scope
            - targetRef
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
