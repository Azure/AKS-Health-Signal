---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: upgradenodeinprogresses.upgrade.aks.io
spec:
  group: upgrade.aks.io
  names:
    kind: UpgradeNodeInProgress
    listKind: UpgradeNodeInProgressList
    plural: upgradenodeinprogresses
    shortNames:
    - unip
    singular: upgradenodeinprogress
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UpgradeNodeInProgress is the Schema for the upgradenodeinprogresses
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UpgradeNodeInProgressSpec defines the desired state of UpgradeNodeInProgress
            properties:
              nodeRef:
                description: NodeReference contains a reference to a Node
                properties:
                  name:
                    description: Name is the name of the Node
                    type: string
                required:
                - name
                type: object
            required:
            - nodeRef
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
